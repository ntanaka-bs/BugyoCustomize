<Window x:Class="AttendanceSystem.Views.AttendanceImportView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AttendanceSystem.Views"
        mc:Ignorable="d"
        Title="勤務表プロ勤怠データ受入" Height="180" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0"
        ResizeMode="NoResize">

    <Grid Margin="15,15,15,15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="15" />
            <ColumnDefinition Width="110" />
        </Grid.ColumnDefinitions>
        
        <GroupBox Grid.Column="0" Header="受入設定">
            <Grid Margin="15,15,15,15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="受入元ファイル名" Margin="0,0,0,5"/>
                
                <TextBox Grid.Row="1" Grid.Column="0" Height="22" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                <Button Grid.Row="1" Grid.Column="1" Content="参照(_B)..." Width="75" Height="22" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>

        <Button Grid.Column="2" Content="受入開始(_E)" Height="24" VerticalAlignment="Top" Margin="0,25,0,0"/>

        <Button Grid.Column="2" Content="キャンセル" Height="24" VerticalAlignment="Bottom" Margin="0,0,0,5"/>
        
    </Grid>
</Window>
